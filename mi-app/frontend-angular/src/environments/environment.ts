// Configuración de entorno para Angular
// Aquí definimos las URLs de la API del backend

export const environment = {
  production: false,
  // URL base del backend API (Node.js + Express)
  apiUrl: 'http://localhost:3000/api'
};
